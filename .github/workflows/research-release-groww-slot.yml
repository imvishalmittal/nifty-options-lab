name: Research - Release Groww API slot

on:
  push:
    branches: [research/entry-relative-risk]
    paths: [research/requests/release-groww-slot.json]

permissions: {}

concurrency:
  group: groww-opportunity-backtest-api
  cancel-in-progress: true

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Released obsolete Groww research run"
